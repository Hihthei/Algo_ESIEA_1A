#include"echangeChaine.h"

void echangeChaine(char* chaine, int n) {

    // char reverse[];
    int a = n;
    for (int i = 0; i < n; i++)
    {
        reverse[a] = chaine[i];
        a--;
    }

    return;
}