switch (a)
{
    case 0:
        printf( "Ca vaut 0\n" );
        break;

    case 1:
        printf( "Ca vaut 1\n" );
        break;

    case 2:
        printf( "%d\n", a );
        break;

    case 3:
        printf( "%d\n", a );
        break;

    case 4:
        printf( "%d\n", a );
        break;

    case 5:
        printf( "%d\n", a );
        break;

    default:
        printf( "Ca vaut un truc\n" );
        break;

}


/*
if ( a == 0 )
    printf( "Ca vaut 0\n" );
if ( a == 1 )
    printf( "Ca vaut 1\n" );
if ( a == 2 )
    printf( "%d\n", a );
if ( a == 3 )
    printf( "%d\n", a );
if ( a == 5 )
    printf( "%d\n", a );
if ( a == 4 )
    printf( "%d\n", a );
if ( (a!=0) && (a!=1) && (a!=2) && (a!=3) && (a!=4) && (a!=5) )
    printf( "Ca vaut un truc\n" );

*/


//le programme n'est pas sencé compiler /!\ /!\
// c'est juste là pour changer les if en switch et switch en if