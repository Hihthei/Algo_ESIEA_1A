// Les trois lignes suivantes sont données ici pour information. Laissez-les en commentaire
// int poids;
// printf ("Quel est le poids de Linux?\n");
// scanf ("%d", &poids);

// Modifiez les lignes de code suivantes pour respecter la consigne
if(poids/10 == 0) printf ("C'est pas un lapin!\n");

else if(poids/10 == 1) printf ("Anorexique!\n");

else if(poids/10 == 2) printf ("Maigre!\n");

else if(poids/10 == 3) printf ("Parfait!\n");

else if(poids/10 == 4) printf ("Gros!\n");

else printf ("C'est pas un ours!\n");

// Le code n'est pas fait pour compiler /!\ /!\